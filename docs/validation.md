# Проверка редакции репозитория от 18.09.2026

Исходный коммит: `1737ca4448a43b05f1f56b1a2f82e19a2cf14255`.
Научный источник и его хеш указаны в [source-manifest.json](source-manifest.json).

## Локально выполнено

| Проверка | Результат |
|---|---|
| `lake build` | Успешно: 9 модулей проекта импортированы корневым модулем |
| `python3 scripts/check_axioms.py` | 295 логических объявлений; разрешённые транзитивные аксиомы только `propext`, `Classical.choice`, `Quot.sound` |
| `python3 scripts/check_repository.py` | Полное покрытие импортами, закреплённые зависимости, отсутствие запрещённых конструкций |
| Тесты защитного скрипта | 3 теста пройдены |
| Gradle `test installDist` | Сборка, упаковка и 25 тестов JUnit пройдены; ошибок и пропусков нет |
| CLI на `examples/valid-mapping.ttl` | Две выбранные проверки пройдены, хеши файлов выведены |
| Gradle Wrapper | JAR совпадает с опубликованным SHA-256; дистрибутив закреплён отдельным SHA-256 |
| Локальные ссылки актуальной документации | Разорванных ссылок не обнаружено |
| `git diff --check` | Ошибок пробелов не обнаружено |

Среда: Linux x86-64, Lean 4.19.0, mathlib
`c44e0c8ee63ca166450922a373c7409c5d26b00b`, JDK 17, Gradle 8.10.2,
Scala 3.3.3, Jena 4.10.0. Зависимости Java/Scala закреплены в
`asg-core/gradle.lockfile`. Служебная настройка сетевого доступа локальной
среды не входит в репозиторий и не отключала проверку TLS.

Тесты охватывают отсутствующие и некорректные артефакты, отсутствие области
проверки, отключённые формы, неподдерживаемые SPARQL/JS-конструкции форм,
удалённый SERVICE (включая вложенный запрос и EXISTS), неверный UTF-8,
расхождение смысла при структурно корректном графе, неполные/дублирующие
свидетельства, привязку согласования к версиям и операции, отзыв, разбиения
атомов, замыкание циклических зависимостей и конфликт требований.

Исходный каталог из 14 Lean-файлов содержал 79 вхождений `sorry` после
исключения вложенных комментариев, строк и однострочных комментариев.
Для новой редакции основным свидетельством служит аудит реальных зависимостей
объявлений, а не одно текстовое сравнение.

## Пределы вывода

Проверка не устанавливает промышленную готовность, соответствие всем требованиям
ГОСТ, фактическое внедрение в СМЭВ, безопасность полного автономного комплекса,
измеренный SLA или экономический эффект. Проверены именно указанные формальные
результаты и лабораторные сценарии. Scala-реализация не извлечена автоматически
из Lean и не имеет доказательства эквивалентности машинному коду.

Статус GitHub Actions следует смотреть у конкретного коммита или PR.
Этот локальный протокол не подменяет результат удалённого CI.
